-use yii\bootstrap\ActiveForm;
-use yii\helpers\Html;

-$view->beginContent('@zoocreative/user/views/admin/update.haml', ['user' => $user])

-$form = ActiveForm::begin(['layout' => 'horizontal','enableAjaxValidation' => true,'enableClientValidation' => false,'fieldConfig' => ['horizontalCssClasses' => ['wrapper' => 'col-sm-9',],],]);

!= $form->field($profile, 'name')
!= $form->field($profile, 'public_email')
!= $form->field($profile, 'website')
!= $form->field($profile, 'location')
!= $form->field($profile, 'gravatar_email')
!= $form->field($profile, 'bio')->textarea()

.form-group
    != Html::submitButton(Yii::t('user', 'Update'), ['class' => 'button expanded'])

-ActiveForm::end();

-$view->endContent()
